# from django.urls import path
# from django.conf.urls import url
# from rest_framework.urlpatterns import format_suffix_patterns
# from .views import cook_list, get_area_list, CookView, CookImageView #user_list, create_cook, CookView

# urlpatterns = [
#     path('cooks_list/', cook_list),
#     path('area_list/', get_area_list),
#     path('cook_details/', CookView.as_view(), name="list"),
#     path('cook_image/', CookImageView.as_view(), name="image"),
#     # path('cook_details/<int:id>', CookView.as_view(), name="update")
#     # url(r'^cook_update/(?P<id>[0-9]{2})/$', CookView.as_view(), name='put_cook'),

# ]

# urlpatterns = format_suffix_patterns(urlpatterns)
